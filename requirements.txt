requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
playwright>=1.53.0
fastapi>=0.115.0
uvicorn>=0.30.0
# LLM providers — install only the one(s) you need:
google-generativeai>=0.8.0
openai>=1.0.0

# Alert delivery — optional, install only if using alerts/
# python-telegram-bot>=21.0   # Telegram bot (HISAAB_TELEGRAM_TOKEN)
# resend>=2.0.0               # Email via Resend API (HISAAB_RESEND_KEY)
